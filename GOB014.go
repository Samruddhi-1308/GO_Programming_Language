//Inserting text Between Outputs
//If we want any other text in between the outputs we can provide that text inside " " sign.

package main
import "fmt"

func main() {
	fmt.Print(3 + 4)
	fmt.Print(" and ") //Replace the underscore (_) and complete the code
	fmt.Print(2 + 1)
}
